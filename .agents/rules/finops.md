---
trigger: always_on
description: FinOps Foundation & cloud cost optimization expertise rules for Cleo.
---

## Cloud FinOps Expertise

This project has the `cloud-finops` skill installed at `.agents/skills/cloud-finops/`.

Rules:
- Ground all cost optimization advice in the FinOps Foundation Framework (Inform → Optimize → Operate) and OptimNow doctrine.
- Diagnose before prescribing: analyze driver granularity, billing dataset mechanics (FOCUS 1.2, CUR, Consumption Breakdown), and customer CRNs before prescribing reduction actions.
- Consult `.agents/skills/cloud-finops/references/` for multi-cloud billing mechanics across AWS (CUR, Bedrock, Savings Plans), Azure (Reservations, AHB, PTUs), GCP, Kubernetes (OpenCost, Kubecost), and AI token economics.
- Consult `.agents/skills/cloud-finops/playbooks/` for executable detection queries for named waste patterns (gp2 to gp3, oversized RDS, Graviton candidates, zombie NAT gateways, unattached EBS, snapshot sprawl, idle ELBs, cross-AZ egress).
- Distinguish Quick Wins (immediate, low-risk, e.g. storage tiering, unattached resources) from Strategic Modernization (architectural, e.g. Graviton, commercial DB license modernization, serverless tuning).

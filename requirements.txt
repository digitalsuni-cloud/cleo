# Core (Required for Cleo & CloudHealth MCP)

# AI Engines (Optional — install only the engine you intend to use)
# mlx-lm>=0.18.0             # Local Apple Silicon models (Qwen 32B / 7B)
# openai>=1.40.0             # OpenAI GPT-4o
# anthropic>=0.34.0          # Anthropic Claude Sonnet
# google-genai>=1.0.0        # Google Gemini (Optional)

# GUI & Server Mode
fastapi>=0.112.0
uvicorn[standard]>=0.30.0
pywebview>=5.0.0
zstandard>=0.22.0
